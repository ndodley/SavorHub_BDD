target/
.idea/
*.iml
.classpath
.project
.settings/

# Local test config (real credentials) — never commit this file.
# Copy config.properties.example to config.properties and fill in real values.
src/test/resources/config.properties
